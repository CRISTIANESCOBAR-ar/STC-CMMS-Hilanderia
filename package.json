{
  "name": "temp-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:main": "npm run build && firebase deploy --only hosting:main",
    "deploy:legacy": "npm run build && firebase deploy --only hosting:legacy",
    "deploy:all": "npm run build && firebase deploy --only hosting:main,hosting:legacy",
    "migrate:sectors:dry": "node scripts/migrate-sectors.mjs",
    "migrate:sectors:apply": "node scripts/migrate-sectors.mjs --apply",
    "import:codigos:dry": "node scripts/import-codigos-textiles.mjs",
    "import:codigos:apply": "node scripts/import-codigos-textiles.mjs --apply"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "exceljs": "^4.4.0",
    "file-saver": "^2.0.5",
    "firebase": "^12.10.0",
    "lucide-vue-next": "^0.577.0",
    "sweetalert2": "^11.26.23",
    "tailwindcss": "^4.2.1",
    "tippy.js": "^6.3.7",
    "vue": "^3.5.30",
    "vue-router": "^5.0.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.5",
    "firebase-admin": "^13.7.0",
    "vite": "^8.0.0"
  }
}
